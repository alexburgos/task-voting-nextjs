import React from 'react';
import StyledFooter from '../styles/StyledFooter';

const Footer = () => (
	<StyledFooter>
		<p>
			Made with 🖤 by Alex Burgos, thanks to <a href="https://nexts.org">Next.js</a> and <a href="https://pusher.com">Pusher</a>
		</p>
	</StyledFooter>
);

export default Footer;
